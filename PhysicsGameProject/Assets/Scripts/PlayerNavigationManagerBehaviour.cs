using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerNavigationManagerBehaviour : MonoBehaviour
{
    [SerializeReference] private PlayerNavigationBehaviour _playerNavigationBehaviour;
    [SerializeReference] private CameraRaycastBehaviour _cameraRaycastBehaviour;
    
    // Start is called before the first frame update
    void Start()
    {
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
